import {Abhaya_Libre} from "next/font/google";


export const abhayaLibre = Abhaya_Libre({
   
    weight: ["400","500"], // You can add more weights as needed, e.g., "400", "700"
    subsets: ["latin"], // Specify subsets for language support
    display: "swap" // Ensures the font loads quickly with a fallback
  });